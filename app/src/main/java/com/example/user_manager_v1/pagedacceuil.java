package com.example.user_manager_v1;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.widget.TextView;
// zyaaaaDaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
public class pagedacceuil extends AppCompatActivity {
    //TextView Montant_des_imayesss;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_pagedacceuil);

       /* Montant_des_imayesss = findViewById(R.id.Montant_des_imayesss);
        Double Montant_des_imayess = getIntent().getDoubleExtra("montant_des_imayes", 0.0); // Use 0.0 as the default value
        // Convert the Double to a String
        String textToDisplay = String.valueOf(Montant_des_imayess);
        // Set the text in the TextView
        Montant_des_imayesss.setText(textToDisplay);*/
    }
}